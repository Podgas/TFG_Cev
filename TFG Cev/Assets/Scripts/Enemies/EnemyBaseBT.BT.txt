tree("Root")
	fallback
		while
			not
				isAlive
			Die
		parallel
			
			tree("patrol")
			tree("enterCombat")
		tree("combat")
		

tree("patrol")
	fallback
		while
			isPatroling
			PatrolBehave
		while
			isWaiting
			Wait
		while
			isAlert
			Alert
				

tree("enterCombat")
	while
		isChasing
		SearchForCombat

	
