tree("Root")
	fallback
		while
			not
				isAlive
			Die
		parallel
			
			tree("patrol")
			tree("enterCombat")	

tree("patrol")
	fallback
		while
			isPatroling
			PatrolBehave
		while
			isWaiting
			Wait
		while
			isAlert
			Alert
		Chase
				

tree("enterCombat")
	while
		isChasing
		sequence
			MoveEnemy

	
